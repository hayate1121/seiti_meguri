<div class="container">
  <div class="row">
    <div class="col">
      いいね一覧
    </div>
  </div>
  <div class="row">
    <% @photos.each do |photo| %>
    <div class="col">
      <%= link_to photo_path(photo.id) do %>
      <%= image_tag photo.get_image(100,100) %>
      <% end %>
    </div>
    <% end %>
  </div>
</div>